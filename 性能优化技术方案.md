# 莫测造境体素编辑器性能优化技术方案

## 1. 目标与范围

本方案针对当前浏览器版莫测造境在以下交互中的卡顿：

- 从资产库拖入实体；
- 水平或竖直移动普通实体、子实体和装配体；
- 鼠标滚轮缩放和右下角缩放控制；
- 编辑模式下的选择、高亮和遮挡处理。

优化后的编辑器必须继续保持：

- 统一 1 mm 逻辑体素；
- X/Y 为场景水平面，Z 为高度的用户界面语义；
- 实体、装配体、嵌套装配体和局部修改能力；
- 资产库模板和场景实例快照隔离；
- 碰撞阻挡、场景边界和网格吸附；
- 单体素添加、擦除和实体编辑；
- 撤销、重做、场景保存和可传播文件；
- STL、普通实体文件和模板实体文件导入导出。

本轮不以切换 WebGPU 为第一目标。当前主要瓶颈来自 CPU 数据路径、Three.js 对象数量和全量场景重建；在这些问题解决前，单纯更换渲染 API 不会根治卡顿。

## 2. 当前实现的主要瓶颈

### 2.1 一个体素对应一个 Three.js Mesh

当前 `buildAssetGroup` 和手动体素渲染路径会为每个体素分别创建：

- `THREE.BoxGeometry`；
- `THREE.MeshStandardMaterial` 或材质克隆；
- `THREE.Mesh`；
- 阴影状态；
- 被选中时的两层线框高亮。

这会使 Draw Call、JavaScript 对象、Geometry 和 Material 数量随体素数量近似线性增长。

### 2.2 高频交互触发完整场景重建

场景渲染 Effect 依赖：

- `project`；
- 选择状态；
- 编辑状态；
- 放置预览状态。

其中任一状态变化都会先 `group.clear()`，然后重新解析并创建场景中全部实体、全部体素和全部高亮对象。资产放置预览每次变化也会重新创建完整预览实体。

旧对象从 Group 中移除后没有逐一释放 Geometry 和 Material，存在 GPU 资源和垃圾对象持续积累的风险。

### 2.3 拖动期间更新持久化 ProjectState

实体每跨越一个逻辑网格，就会执行：

1. 重新解析场景实体；
2. 重新计算移动体素和静态体素；
3. 创建碰撞 Set；
4. `structuredClone` 整个工程；
5. 规范化工程结构；
6. 更新 React 状态；
7. 触发视口重建；
8. 可能触发场景自动保存。

拖动操作本质上是临时预览，但当前使用了与正式场景提交相同的数据路径。

### 2.4 碰撞检测每次重建全场景占用关系

当前碰撞检测以字符串 `"x,y,z"` 作为体素键。资产放置或移动时，会重新将所有实例转换成场景体素，并为每个实例创建新的 `Set<string>`。

其成本大致为：

```text
O(移动实体体素数 + 全场景体素数)
```

并且该成本会在每次有效指针移动时重复发生。

### 2.5 缩放存在双向状态回写

当前缩放链路同时由 OrbitControls 和 React `zoomLevel` 管理：

```text
滚轮
→ OrbitControls 修改相机
→ change 事件写入 React
→ React Effect 再修改相机
```

这会引入额外 React 渲染、相机重复更新和可能的反馈抖动。

### 2.6 编辑遮挡每帧重复射线检测

编辑模式下，每个动画帧都会：

- 遍历编辑实体的全部体素 Mesh；
- 从相机向每个目标 Mesh 发射射线；
- 遍历场景寻找遮挡实体；
- 修改遮挡材质和框线；
- 再执行遮罩及第二次场景渲染。

即使相机和场景完全静止，也会重复执行相同计算。

## 3. 优化后的总体架构

将当前“ProjectState 同时承担持久化、运行时索引和渲染数据”的结构拆成三层：

```text
ProjectState
持久化事实来源
        │
        ▼
SceneRuntime
实体节点、变换缓存、Chunk、占用索引
        │
        ▼
SceneRenderer
批量 Mesh、预览 Mesh、选择框线、相机
```

### 3.1 ProjectState

继续保存：

- 资产和实例；
- 自定义体素；
- 装配体关系；
- 实体名称和颜色；
- 锁定状态；
- 场景边界；
- 文件格式所需数据。

只有已确认的用户操作才能修改 ProjectState。

以下状态不得写入 ProjectState：

- 鼠标当前拖动位移；
- 放置预览位置；
- 碰撞预览结果；
- 相机缩放和旋转中的瞬时值；
- Hover、高亮动画和临时遮挡结果。

### 3.2 SceneRuntime

新增纯运行时对象：

```ts
type SceneRuntime = {
  entities: Map<string, RuntimeEntity>
  chunks: Map<string, RuntimeChunk>
  occupancy: SceneOccupancyIndex
  assetTransforms: AssetTransformCache
  renderRevisions: Map<string, number>
}
```

它由 ProjectState 初始化，但不会进入 `.moceworld` 文件。

### 3.3 SceneRenderer

SceneRenderer 只接收运行时增量事件：

```ts
entityAdded
entityRemoved
entityTransformChanged
entityVoxelsChanged
selectionChanged
cameraChanged
chunkMeshReady
```

不再通过“整个 ProjectState 改变”来重建整个 Three.js Group。

## 4. Chunk 与体素数据结构

### 4.1 坐标约定

当前持久化数据中的体素轴和界面轴存在历史映射。运行时层应建立明确的规范坐标：

```ts
type RuntimeVoxelCoord = {
  gx: number // 场景水平 X
  gy: number // 场景水平 Y
  gz: number // 场景高度 Z
}
```

在 ProjectState 和 SceneRuntime 之间只允许通过集中转换函数映射：

```text
Project voxel.x → Runtime gx
Project voxel.z → Runtime gy
Project voxel.y → Runtime gz
```

渲染、碰撞、边界和 DDA 射线检测统一使用 Runtime 坐标，避免后续继续出现轴向混用。

### 4.2 Chunk 尺寸

采用原计划中的 `32 × 32 × 32`：

```ts
const CHUNK_SIZE = 32
const CHUNK_VOLUME = 32768
```

只存储非空 Chunk。

### 4.3 RuntimeChunk

建议结构：

```ts
type RuntimeChunk = {
  key: string
  cx: number
  cy: number
  cz: number

  occupancyBits: Uint32Array // 32768 bit，约 4 KB
  materialIds: Uint8Array   // 每体素材质 ID，32 KB
  ownerIds: Uint32Array     // 每体素运行时实体 ID，128 KB

  dataRevision: number
  meshRevision: number
  meshState: 'clean' | 'dirty' | 'building'
}
```

局部坐标可以打包为：

```ts
const localIndex = lx | (ly << 5) | (lz << 10)
```

其中 `lx`、`ly`、`lz` 均为 `0...31`。

### 4.4 实体 ID 映射

文件中的字符串 ID 保持不变。运行时为每个实体分配整数 Handle：

```ts
type EntityHandle = number

entityIdToHandle: Map<string, EntityHandle>
handleToEntityId: string[]
```

碰撞和选择查询使用整数，文件树和持久化使用字符串。

## 5. 资产变换缓存

资产的体素拓扑在移动期间不会变化。只要以下属性不变，就不应重新执行逐体素旋转和镜像：

- 资产 ID 和资产版本；
- 体素 Override 版本；
- X/Y/Z 镜像；
- X/Y/Z 旋转；
- 子部件偏移。

缓存键：

```ts
type TransformCacheKey =
  `${assetId}:${assetRevision}:${overrideRevision}:${mirrorKey}:${rotationKey}:${partOffsetRevision}`
```

缓存结果：

```ts
type TransformedAssetCache = {
  voxels: Int16Array | Int32Array
  bounds: RuntimeAabb
  partRanges: Map<string, { start: number; count: number }>
}
```

实体平移时，只给缓存中的局部整数坐标增加场景原点，不再做三角函数、浮点换算和四舍五入。

## 6. 增量碰撞系统

### 6.1 两阶段检测

先做宽阶段，再做窄阶段：

```text
移动实体 AABB
→ 查找相交 Chunk
→ 查询 occupancyBits
→ 仅对可能重叠的体素做精确检测
```

### 6.2 排除自身

拖动实体时不需要重新创建“除自己外的全场景 Set”。占用索引保存 owner Handle：

```ts
if (owner !== EMPTY && !movingHandles.has(owner)) {
  collision = true
}
```

这样可以直接支持：

- 单独实体；
- 多选实体；
- 完整装配体；
- 装配体中的部分子实体；
- 当前编辑实体。

### 6.3 碰撞查询接口

```ts
type CollisionResult = {
  valid: boolean
  blockedAxis?: 'x' | 'y' | 'z'
  blockedDirection?: -1 | 1
  collidingEntityIds: string[]
}

function testEntityTranslation(
  runtime: SceneRuntime,
  entityHandles: EntityHandle[],
  delta: RuntimeVoxelCoord,
): CollisionResult
```

### 6.4 更新时机

占用索引只在以下情况更新：

- 实体正式放入场景；
- 拖动结束并提交；
- 添加或删除体素；
- 镜像或旋转成功提交；
- 删除实体；
- 打开或新建场景。

拖动预览期间不修改静态占用索引。

## 7. 拖动与放置事务

### 7.1 DragSession

```ts
type DragSession = {
  pointerId: number
  entityHandles: EntityHandle[]
  startOrigins: Map<EntityHandle, RuntimeVoxelCoord>
  requestedDelta: RuntimeVoxelCoord
  appliedDelta: RuntimeVoxelCoord
  valid: boolean
  historySnapshotTaken: boolean
}
```

### 7.2 交互流程

```text
pointerdown
→ 创建 DragSession
→ 将移动实体加入临时 DragGroup

pointermove
→ 只保留最新指针坐标
→ requestAnimationFrame 中最多处理一次
→ 计算目标网格
→ 查询占用索引
→ 更新 DragGroup.position
→ 更新红色/正常预览状态

pointerup
→ 将最终整数位移一次性写入 ProjectState
→ 更新占用索引
→ 写入一次 Undo
→ 标记一次保存
→ 销毁 DragSession
```

### 7.3 资产库拖入

资产卡片开始拖动时：

- 从资产渲染缓存创建一个 Ghost Renderable；
- Ghost 只创建一次；
- 移动时只更新 Ghost 的整体变换和颜色；
- 不更新 React `placementPreview`；
- 不在每次移动中重新创建资产体素 Mesh；
- 松开鼠标后才创建场景实例快照和正式运行时实体。

### 7.4 多选和装配体

拖动时将所选实体挂入临时父 Group，保留各自局部变换：

```text
DragGroup
├── Entity A render node
├── Entity B render node
└── Assembly C render node
```

结束后再把位移分别写回实体或子部件偏移。装配体关系本身不因拖动而改变。

## 8. 渲染优化

### 8.1 第一阶段：InstancedMesh 过渡

在完整 Chunk Mesher 完成前，先把同一材质的体素合并到 `THREE.InstancedMesh`：

```text
一个实体或 Chunk
├── material 1 → InstancedMesh
├── material 2 → InstancedMesh
└── material 3 → InstancedMesh
```

每个体素只写入实例矩阵和实例颜色，不再创建独立 Geometry、Material 和 Mesh。

Raycast 返回的 `instanceId` 可以映射回逻辑体素坐标。

### 8.2 第二阶段：隐藏面剔除

对于每个占用体素，只生成与空体素相邻的面。实体内部和相邻实体接触处的不可见面不进入渲染 Geometry。

逻辑体素仍完整保留，剔除只影响渲染。

### 8.3 第三阶段：Greedy Meshing

对每个 Chunk 的六个方向分别扫描二维切片，将以下条件相同的相邻面合并：

- 朝向相同；
- 材质相同；
- 透明状态相同；
- 所属渲染层相同。

输出：

```ts
type ChunkMeshPayload = {
  positions: Float32Array
  normals: Int8Array
  colors: Uint8Array
  indices: Uint32Array
  materialGroups: Array<{ start: number; count: number; materialId: number }>
  revision: number
}
```

每个 Chunk 生成一个或少量 `BufferGeometry`，Draw Call 与 Chunk/材质数量相关，不再与体素数量相关。

### 8.4 选中和编辑框线

不能再为每个体素创建两套 `LineSegments`。

对选中实体单独生成一个线段 Buffer：

```text
当前视角可见的体素表面
→ 收集其体素边
→ 去重
→ 一个 LineSegments
→ depthTest = true
```

这样仍能显示“所有可见体素方块的外框线”，且不会透视，也只需要一个 Draw Call。

框线只在以下变化时重建：

- 选择对象改变；
- 编辑对象体素改变；
- 相机角度跨越需要更新的阈值；
- 选中对象完成旋转或镜像。

### 8.5 阴影和像素比

交互期间采用动态质量：

```text
静止状态：
  pixelRatio ≤ 1.5
  允许阴影

拖动/缩放/旋转：
  pixelRatio = 1
  暂停体素 castShadow
  保留基础光照

交互结束：
  恢复静止质量
```

第一版可以直接关闭逐体素实时阴影，仅保留地面接触阴影或低成本环境光。

### 8.6 按需渲染

当前视口持续 `requestAnimationFrame`。改为：

```ts
invalidate(RenderReason.Camera)
invalidate(RenderReason.Scene)
invalidate(RenderReason.Selection)
invalidate(RenderReason.Animation)
```

只有以下情况持续渲染：

- OrbitControls 仍有阻尼；
- 摇杆正在拖动；
- 实体正在拖动；
- 有进行中的视觉动画。

静止后停止动画循环，直到下一次 `invalidate`。

## 9. 缩放状态重构

### 9.1 单一实时来源

相机是实时缩放的唯一事实来源：

```ts
cameraZoomRef.current
```

React `zoomLevel` 只用于显示右下角百分比，不再反向驱动高频相机更新。

### 9.2 滚轮流程

```text
wheel
→ OrbitControls 原生连续缩放
→ 限制到 50%～2000%
→ requestAnimationFrame 最多同步一次百分比
→ 不更新 ProjectState
```

点击加减按钮调用同一个相机缩放函数：

```ts
setCameraZoom(nextPercent, { animate: false })
```

超过 100% 后，按钮档位为 50%；无级滚轮仍保留连续值。

### 9.3 回中

回中操作必须同时：

- 清除 OrbitControls 阻尼速度；
- 恢复默认目标点；
- 恢复默认相机方向；
- 恢复 100%；
- 立即同步右下角百分比；
- 触发一次渲染。

## 10. 射线选择和体素编辑

### 10.1 体素 DDA

点击体素不再对数千个 Mesh 执行 `Raycaster.intersectObject`，而是：

```text
相机射线
→ 与场景 AABB 求交
→ 进入第一个网格
→ 3D DDA 逐格前进
→ 查询 occupancyBits
→ 返回首个命中的 owner 和体素坐标
```

DDA 查询直接得到：

- 命中实体；
- 命中体素；
- 命中面法线；
- 添加体素的相邻坐标；
- 擦除目标。

### 10.2 实体选择

命中体素的 `ownerIds` 可以映射到：

- 普通实体；
- 模板实例；
- 子实体；
- 当前装配体路径。

文件树选择和场景选择继续共享同一 Entity ID。

### 10.3 BVH 的使用边界

对于导入的非体素网格预览或合并后的复杂 Chunk Mesh，可以使用 `three-mesh-bvh` 加速 Raycast。纯体素编辑优先使用 DDA，因为它能直接得到准确网格坐标和面方向。

## 11. Worker 设计

### 11.1 持久 Chunk Worker

新增长期存在的 Worker，不要每次请求都创建和销毁：

```text
src/workers/chunk-mesh.worker.ts
```

消息协议：

```ts
type ChunkWorkerRequest = {
  type: 'build-chunk'
  chunkKey: string
  revision: number
  occupancy: ArrayBuffer
  materialIds: ArrayBuffer
  neighborBorders: ArrayBuffer[]
}

type ChunkWorkerResponse = {
  type: 'chunk-built'
  chunkKey: string
  revision: number
  mesh: ChunkMeshPayload
}
```

### 11.2 版本防抖

如果 Chunk 在 Worker 计算期间再次被修改：

- `dataRevision` 增加；
- 旧结果返回时发现版本不匹配；
- 直接丢弃旧 Mesh；
- 调度最新版本。

### 11.3 Transferable

体素和 Mesh 数据使用可转移的 `ArrayBuffer`，避免复制大量对象。ProjectState 仍保留普通结构用于保存，但运行时 Worker 不传递完整 ProjectState。

### 11.4 优先级队列

Chunk 构建优先级：

1. 当前编辑 Chunk；
2. 当前拖动实体附近 Chunk；
3. 相机视锥内 Chunk；
4. 其他脏 Chunk。

## 12. 编辑模式遮挡优化

遮挡结果仅在以下事件后重新计算：

- 相机矩阵变化；
- 编辑目标变化；
- 场景实体变换；
- Chunk Mesh 更新。

建议两种实现：

### 方案 A：屏幕空间遮罩

- 普通实体正常渲染但乘以暗色；
- 编辑实体单独渲染到 Overlay；
- 深度测试判断遮挡；
- 遮挡实体只在需要时改为线框代理。

### 方案 B：低频 BVH 遮挡查询

- 为实体或 Chunk 建立 BVH；
- 对编辑实体的包围盒采样有限数量射线；
- 只在相机变化后更新遮挡实体集合；
- 不再对每个体素逐帧发射射线。

第一版推荐方案 B，但以实体包围盒采样代替逐体素射线。

## 13. 保存与历史记录

### 13.1 保存触发

只有正式场景提交触发 Dirty：

- 添加或删除体素；
- 完成实体拖动；
- 完成放置；
- 完成旋转、镜像或颜色修改；
- 装配体结构修改；
- 名称、锁定和边界修改。

相机、Hover、临时预览和碰撞失败不触发保存。

### 13.2 自动保存

建议：

```text
正式提交
→ 标记 dirty
→ 1000～1500 ms 防抖
→ Worker 或空闲时段序列化
→ 后端保存
```

拖动开始时暂停自动保存；拖动结束后恢复并保存一次。

### 13.3 Undo

一次连续拖动只产生一个历史记录：

```text
before origins
after origins
affected entity IDs
```

长期可将历史记录从完整 ProjectState 快照改为命令式 Patch，但不作为第一轮必需项。

## 14. 文件拆分建议

当前 `src/main.tsx` 同时负责界面、运行时、渲染和交互，应分拆为：

```text
src/
├── runtime/
│   ├── scene-runtime.ts
│   ├── runtime-coordinates.ts
│   ├── asset-transform-cache.ts
│   ├── spatial-index.ts
│   ├── drag-session.ts
│   └── runtime-types.ts
├── rendering/
│   ├── scene-renderer.ts
│   ├── chunk-renderer.ts
│   ├── instanced-voxel-renderer.ts
│   ├── selection-outline.ts
│   ├── camera-controller.ts
│   └── render-scheduler.ts
├── meshing/
│   ├── culled-mesher.ts
│   ├── greedy-mesher.ts
│   └── mesh-types.ts
├── interaction/
│   ├── voxel-raycast.ts
│   ├── entity-drag-controller.ts
│   ├── placement-controller.ts
│   └── selection-controller.ts
└── workers/
    └── chunk-mesh.worker.ts
```

`src/voxel.ts` 继续保留文件格式相关的纯数据函数，逐步把运行时专用逻辑迁出。

## 15. 实施阶段

### 阶段 0：建立性能基线，0.5～1 天

- 增加仅开发环境显示的性能 HUD；
- 记录 FPS、Draw Call、Triangles、Geometry、Texture；
- 使用 `performance.mark/measure` 记录拖动、碰撞和场景重建耗时；
- 建立固定压力场景。

输出：

```text
baseline.json
性能测试场景
录屏或浏览器 Performance Profile
```

### 阶段 1：资源泄漏和低风险快速优化，1～2 天

- 完整释放被移除的 Geometry 和 Material；
- 复用单位 BoxGeometry；
- 复用基础材质；
- 拖动期间关闭阴影；
- 限制交互像素比；
- 自动保存不在拖动中执行；
- 编辑遮挡从每帧改为事件驱动。

这一阶段不改变文件格式和实体逻辑。

### 阶段 2：交互状态与 ProjectState 分离，2～4 天

- 实现 DragSession；
- 实体拖动只改 Three.js 临时变换；
- 放置预览只使用 Ghost Renderable；
- pointerup 一次性提交；
- 一次拖动只生成一个 Undo；
- 相机缩放使用单一实时状态。

该阶段预计能显著改善拖动和缩放手感。

### 阶段 3：运行时占用索引，3～5 天

- 建立 Runtime 坐标层；
- 建立 32³ Chunk；
- 建立 occupancy、material 和 owner TypedArray；
- 实现增量插入、删除和移动；
- 碰撞检测切换到索引；
- 增加碰撞一致性测试。

### 阶段 4：InstancedMesh 渲染，3～5 天

- 资产体素改为按实体/材质实例化；
- 手动体素改为实例化；
- 实现 instanceId 到逻辑体素映射；
- 高亮改为单个 LineSegments Buffer；
- 移除逐体素 Geometry 和 Material。

### 阶段 5：Chunk Meshing 与 Worker，5～8 天

- 实现隐藏面剔除；
- 实现 Chunk Mesh；
- 建立持久 Worker；
- 实现脏 Chunk 增量重建；
- 实现材质组或顶点颜色；
- 再实现 Greedy Meshing。

### 阶段 6：射线、编辑态和收尾，3～5 天

- 实现体素 DDA；
- 替换逐 Mesh Raycast；
- 重构编辑遮挡；
- 完成多选、装配体、锁定、拖入和体素编辑回归；
- 补齐性能测试和内存稳定测试。

单人预计约 3～5 周。若先完成阶段 1～4，可在约 2 周内获得大部分交互性能收益。

## 16. 测试方案

### 16.1 单元测试

- Runtime 与 Project 坐标双向转换；
- Chunk 键和局部索引；
- 占用位设置、读取和清除；
- owner 排除自身的碰撞检测；
- 多选实体整体移动；
- 装配体和子实体移动；
- 边界阻挡；
- 镜像和 90/180/270 度旋转缓存；
- 脏 Chunk 邻接更新；
- Greedy Mesher 不丢面、不生成内部面；
- DDA 返回正确体素和面法线；
- 拖动只生成一次历史记录。

### 16.2 回归测试

- 资产拖入后颜色和名称不受模板后续修改影响；
- 当前编辑实体的新增体素仍属于该实体；
- 删除体素导致实体分裂后可以分别移动；
- 装配体层级和命名保持不变；
- 固定实体不可移动、旋转或镜像；
- 选中框线不透视；
- 场景保存、打开后内容一致；
- STL 和实体文件导出结果不受渲染合并影响。

### 16.3 浏览器性能测试

建立三档固定场景：

```text
小：10,000 体素
中：100,000 体素
大：500,000 体素
```

分别测试：

- 相机旋转；
- 50%～2000% 连续缩放；
- 单实体拖动；
- 多选实体拖动；
- 资产库拖入；
- 编辑模式添加和擦除；
- 选择高亮；
- 场景打开和保存。

## 17. 量化验收标准

以桌面 Chromium 和常见 Apple Silicon Mac 为主要验收环境：

- 中型场景旋转、缩放、拖动平均不低于 55 FPS；
- 指针移动主线程处理时间 P95 小于 4 ms；
- 碰撞查询 P95 小于 3 ms；
- 拖动期间不执行完整 ProjectState Clone；
- 拖动期间不发送场景保存请求；
- 一次拖动只产生一次 Undo；
- Draw Call 与可见 Chunk/材质数量相关，不与体素总数线性增长；
- 中型场景常态 Draw Call 建议控制在 200 以内；
- 连续拖动 60 秒后 `renderer.info.memory.geometries` 保持稳定；
- 连续缩放 60 秒后内存不持续增长；
- 静止时不重复执行编辑遮挡射线；
- 打开旧 `.moceworld` 文件不需要迁移或损失数据；
- STL 导出体素内容与优化前一致。

## 18. 技术路线决策

### 立即采用

- ProjectState 与交互状态分离；
- 32³ Sparse Chunk；
- TypedArray 占用索引；
- 资产变换缓存；
- InstancedMesh；
- 隐藏面剔除；
- Greedy Meshing；
- Worker 增量构网；
- DDA 体素射线；
- 事件驱动遮挡计算；
- 按需渲染。

### 暂缓采用

- 整体迁移 WebGPU；
- GPU Compute Meshing；
- Sparse Voxel Octree 实时渲染；
- 浏览器端完整 OpenVDB；
- OffscreenCanvas 承担完整 Three.js 渲染；
- ECS 全量重构；
- Undo 系统立即改为完整 Command/Delta 架构。

这些技术适合更大规模的无限世界或百万级以上动态场景，但当前应先消除现有全量重算和逐体素对象问题。

## 19. 推荐开工顺序

最终建议按以下顺序施工：

```text
性能基线
→ GPU 资源释放
→ 拖动事务
→ 缩放单一状态
→ 占用索引
→ InstancedMesh
→ 隐藏面剔除
→ Chunk Worker
→ Greedy Meshing
→ DDA 选择
→ 编辑遮挡重构
→ 全量性能与回归验收
```

第一轮施工应优先完成“拖动事务 + 占用索引 + InstancedMesh”。这三项直接对应当前最明显的拖入、移动和缩放卡顿。

## 20. 技术参考

- MagicaVoxel 官方：<https://ephtracy.github.io/>
- Goxel 开源仓库：<https://github.com/guillaumechereau/goxel>
- Three.js InstancedMesh：<https://threejs.org/docs/pages/InstancedMesh.html>
- Three.js 大量对象优化：<https://threejs.org/manual/en/optimize-lots-of-objects.html>
- Greedy Meshing：<https://0fps.net/2012/06/30/meshing-in-a-minecraft-game/>
- Binary Greedy Meshing 示例：<https://github.com/cgerikj/binary-greedy-meshing>
- three-mesh-bvh：<https://github.com/gkjohnson/three-mesh-bvh>
- OpenVDB Overview：<https://www.openvdb.org/documentation/doxygen/overview.html>
- Efficient Sparse Voxel Octrees：<https://research.nvidia.com/publication/2010-02_efficient-sparse-voxel-octrees>
- Three.js WebGPURenderer：<https://threejs.org/manual/en/webgpurenderer>

Goxel 采用 GPL3；可以参考其数据组织和交互思想，但不应直接将其源码复制进商业项目。
